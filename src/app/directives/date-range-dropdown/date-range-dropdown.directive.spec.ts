import { DateRangeDropdownDirective } from './date-range-dropdown.directive';

describe('DateRangeDropdownDirective', () => {
	it('should create an instance', () => {
		const directive = new DateRangeDropdownDirective();
		expect(directive).toBeTruthy();
	});
});
